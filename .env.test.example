# Test user credentials — used by /test-feedback skill
# Real values live in .env.test (gitignored). Both partners share these.
# Domain @tradeson.test is the marker we filter out in analytics.

TEST_HOMEOWNER_EMAIL=homeowner@tradeson.test
TEST_HOMEOWNER_PASSWORD=testpass123

TEST_TRADESPERSON_EMAIL=tradesperson@tradeson.test
TEST_TRADESPERSON_PASSWORD=testpass123

TEST_PM_EMAIL=pm@tradeson.test
TEST_PM_PASSWORD=testpass123

# Where the dev server runs (skill checks this is up before running tests)
TEST_BASE_URL=http://localhost:5173
